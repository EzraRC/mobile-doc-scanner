import 'package:flutter/material.dart';

class editScanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Edit Scan'),
      ),
      body: Center(
        child: Text(
          'Text',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
