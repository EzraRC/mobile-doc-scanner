import 'package:flutter/material.dart';

class previousScansPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Previous Scans'),
      ),
      body: Center(
        child: Text(
          'Text',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
