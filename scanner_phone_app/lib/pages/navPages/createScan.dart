import 'package:flutter/material.dart';

class createScanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create Scan'),
      ),
      body: Center(
        child: Text(
          'Text',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
